{{!-- May need to add partials here in order to display previous notes and adding a new note --}}


<article 
    class="card w-50 mb-3 d-flex align-items-center mt-3"
    style="display:flex; justify-content:center; border: 1px solid black;"
    >
    <form type="submit" class="card-body" style="display:flex; flex-direction:column; align-items:center;">

        <div class="mb-3">
            
            <label for="makeAPost" class="form-label">
                Write your post here:
            </label>

            <textarea class="form-control" id="makeAPost" cols="100" rows="5"></textarea>
        </div>
        <button class="btn btn-primary mt-3">Post It!</button>
    </form>
</article>

<div class="input-group input-group-lg w-50">
</div>

{{!-- class="d-flex justify-content-center input-group-text w-50 mt-3" --}}